# FIX AS NEEDED

# I like my toolkit

alias dkRa='docker run -e COLUMNS=`tput cols` -e LINES=`tput lines` -it --rm --entrypoint /usr/bin/zsh mindthump/toolkit'

source ~/.venv_py3/bin/activate > /dev/null 2>&1
