alias dksum="docker image ls -a; docker container ls -a; docker volume ls; docker network "
alias dkCla="docker container ls -a"
alias dksum="docker image ls -a; echo; docker container ls -a; echo; docker volume ls; echo; docker network ls"
alias dkClf="docker container list --format 'table {{.ID}}\t{{.Names}}\t{{.Image}}\t{{.Command}}\t{{.Status}}'"
alias podimages="kubctl get pods -o jsonpath='{.items[*].spec.containers[*].image}' | tr -s '[[:space:]]' '\n' | sort -u"

alias lll="exa -l --icons --no-user --tree --level 2 --color always | less"
alias lal="exa -al --icons --no-user --tree --level 2 --color always | less"
alias ydl="youtube-dl --no-check-certificate --ignore-errors"
alias now="date -u +%Y%m%d_%H%M%SZ"
alias -g B='| bat'

alias lgtk='tmux new-window -n TK lazygit -p ~/repos/toolkit'
alias lgpaf='tmux new-window -n PAF lazygit -p ~/repos/python_app_framework'
alias lgdot='tmux new-window -n DOT lazygit -p ~/.dotfiles'
alias lgkj='tmux new-window -n J-K lazygit -p ~/repos/kubernetes-jenkins'

alias k=kubectl
[[ $commands[kubectl] ]] && source <(kubectl completion zsh)

alias kr='kubectl run -i -t --rm --image'
kns ()
{
  kubectl config view --minify -o jsonpath='{..namespace}{"\n"}'
}
alias k8smaster='cd /Users/ed/repos/vagrant-kubeadm-kubernetes; tmux new-window -n k8s-master vagrant ssh master'
alias kgpaw='kubectl get pods -A -o wide | bat -S'
alias kgaaw='kubectl get --all-namespaces --output wide all,pv,pvc,sc,cm | bat -S'
alias kgaaj='kubectl get --all-namespaces --output json all,pv,pvc,sc,cm'
alias kgaay='kubectl get --all-namespaces --output yaml all,pv,pvc,sc,cm'

alias mk='minikube'
alias mkst='minikube start'
alias mks='minikube status'
alias mkstop='minikube stop'
alias mkde='eval $(minikube -p minikube docker-env)'
alias mkIb='minikube image build'
alias mkIls='minikube image ls'
alias mkdash='minikube dashboard'

agc ()
{
  /usr/local/bin/ag --color $@ | less -S
}

lg()
{
    export LAZYGIT_NEW_DIR_FILE=~/.lazygit/newdir

    lazygit "$@"

    if [ -f $LAZYGIT_NEW_DIR_FILE ]; then
            cd "$(cat $LAZYGIT_NEW_DIR_FILE)"
            rm -f $LAZYGIT_NEW_DIR_FILE > /dev/null
    fi
}

alias h=history

alias tpaf='tmux neww -c ~/repos/python_app_framework -n paf'
alias tvkk=' tmux neww -c ~/repos/vagrant-kubeadm-kubernetes -n vkk'
alias ttk=' tmux neww -c ~/repos/toolkit -n tk'
alias k8snode='tmux new-window -c /Users/ed/repos/vagrant-kubeadm-kubernetes -n k8s-node01 vagrant ssh node01 -c byobu'
alias k8smaster='tmux new-window -c /Users/ed/repos/vagrant-kubeadm-kubernetes -n k8s-master vagrant ssh master -c byobu'
